"use strict"

//for loop

/*two types of cycles, defined (we know the end))  and undefined (we don't know when it ends)*/
var products = 5;

for(let count = 0; count < products; count++){
    console.log('Product #' + count);
    debugger;
}